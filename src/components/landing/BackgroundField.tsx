/**
 * BackgroundField — intentionally empty in the editorial system.
 * Each section paints its own environmental color (env-light / env-dark)
 * for crisp, decisive black/white contrast. No global atmosphere.
 */
export default function BackgroundField() {
  return null;
}
